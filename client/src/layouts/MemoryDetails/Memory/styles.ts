import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
  cover: {
    maxWidth: "650px",

    marginLeft: "auto",
    marginRight: "auto",
  },

  upperPart: {
    textAlign: "center",
    padding: "3% 0%",
  },

  title: {
    marginBottom: "0.7rem",
  },
}));
